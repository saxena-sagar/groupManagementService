package ie.tcd.groupManagement.node;

import java.io.BufferedReader;
import java.io.DataInputStream;
import java.io.DataOutputStream;
import java.io.IOException;
import java.io.InputStreamReader;
import java.net.Socket;

public class Handler {
	public Handler(Member member, Socket socket) throws IOException {

		/*String memberId = null;

		String inMessage = dataInput.readUTF();
		String[] commands = inMessage.split(",");
		
		for (String command : commands) 
		{
			String[] messages = command.split(":");
			if(messages[0] == "ID")
			{
				memberId = messages[1];
			}
			if (messages[0] == "CONNECT") {
				System.out.println("HERE for " + messages[1] + " " + member.memberCount);
				System.out.println();
				// ADD to Group
			} else {
				System.out.println("INVALID MESSAGE");
			}
		}*/

	}

}
